---
# roles/transmission/handlers/main.yml

- name: Reload systemd
  systemd:
    daemon_reload: true

- name: Restart transmission-daemon
  systemd:
    name: transmission-daemon
    state: restarted
